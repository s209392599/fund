/*
markRaw
工艺物料打包的时候自动添加版本号

vue3 中Proxy 对数组、对象 到底影响哪些东西？


虚拟列表
虚拟表头

ts的学习
vue的打包优化

性能优化的监听 permance.now之类的


dataZoom都会被强制重置到初始的[0, 20]区间。如果用户在之前已经通过拖拽缩放浏览了其他数据区域，这个行为就会破坏其探索状态，导致用户体验割裂。
{
  notMerge: false,
  replaceMerge: ["xAxis", "series"],
}

largeThreshold是与large属性配合使用的阈值参数，它定义了启用大规模优化模式的数据量下限。当且仅当数据项数超过此阈值时，优化绘制逻辑才会生效；反之，系统将使用标准渲染流程，避免不必要的开销；大多数情况下无需手动调整。
setOption({
  series: [
    {
      type: "bar",
      data,
      large: true,
      largeThreshold: 1000,
    },
  ],
});

v-on传递对象
<button v-on="{ click: onClick, mouseover: onHover }">绑定多个事件</button>

还有哪些事件？
document.startViewTransition(() => {
  // 在这里改 DOM
});

<script type="importmap">
{
  "imports": {
    "react": "https://esm.sh/react@canary",
    "react-dom": "https://esm.sh/react-dom@canary"
  }
}
</script>


*/
